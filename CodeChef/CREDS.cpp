#include <bits/stdc++.h>

using namespace std;
#define ll long long
#define wasif() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

void rtp()
{
    int x, y, z;
    cin >> x >> y >> z;

    cout << x * 4 + y * 2 << "\n";
}

int main()
{
    wasif();
    int T;
    cin >> T;

    while (T--)
    {
        rtp();
    }
    
    return 0;
}